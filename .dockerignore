# docker build context is the project root — keep it lean. The ~1.1 GB
# archive/ clone, the database, git history, and local data backups must
# never be shipped to the Docker daemon or baked into an image.
.git
archive
staged
core/backups
**/__pycache__
**/*.pyc
