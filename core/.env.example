# hone-core — environment template.  cd core && cp .env.example .env
# .env is gitignored — never commit real secrets.

# Fleet secret — gates the OAuth / enrollment API (a node presents it to
# begin enrolling). hone-core and every hone-node must use the SAME value.
# generate:  openssl rand -hex 32
HONE_FLEET_SECRET=

# Admin token — the credential for the built-in config-admin identity (the
# only one with access to /users management). It has no database row; sign
# in via the regular /login form with ANY email address and this token as
# the password. Regular operator accounts use email+password (or Google SSO);
# see HONE_GOOGLE_CLIENT_ID below.
# generate:  openssl rand -hex 32
HONE_ADMIN_TOKEN=

# Session secret — signs the operator UI's session cookies. REQUIRED:
# hone-core refuses to start without it (the alternative is a known-constant
# signing key that any reader of the source could use to forge an admin
# session). Generate with:  openssl rand -hex 32
HONE_SESSION_SECRET=

# TLS termination. Default true: hone-core terminates TLS itself with the
# self-generated certificate (no proxy needed — the supported default). Set
# false to serve plain HTTP behind a TLS-terminating proxy (e.g. nginx on the
# host, see nginx/hone-core.conf); in that mode hone-core also trusts the
# proxy's X-Forwarded-* headers. When false, also set HONE_SESSION_COOKIE_SECURE
# =false and publish the port on loopback only.
# HONE_SERVE_TLS=true

# Session cookie's Secure flag — sent over HTTPS only. Default true.
# hone-core serves HTTPS directly, so the default is fine for dev too. Flip
# to false only when fronting hone-core with a proxy that speaks HTTP to
# the backend (i.e. together with HONE_SERVE_TLS=false).
# HONE_SESSION_COOKIE_SECURE=true

# Google SSO (optional) — leave blank to disable the Google login button.
# Create an OAuth 2.0 Client ID in Google Cloud Console; add the redirect URI:
#   https://<HONE_HOSTNAME>:<HONE_PUBLISH_PORT>/auth/google/callback
HONE_GOOGLE_CLIENT_ID=
HONE_GOOGLE_CLIENT_SECRET=

# Public hostname of this hone-core. It is the SAN of the TLS certificate
# hone-core generates on first start, so nodes — which validate against the
# CA delivered at enrollment — must reach hone-core under exactly this name.
HONE_HOSTNAME=

# Host port to publish hone-core's HTTPS port on (optional; default 8000).
# hone-core always listens on 8000 inside the container — this is the host
# side of the port mapping, and the port in the default HONE_PUBLIC_URL below.
HONE_PUBLISH_PORT=8000

# Base URL nodes and operators reach hone-core at — used for the enrollment
# verification link. Optional; defaults to https://$HONE_HOSTNAME:$HONE_PUBLISH_PORT.
# Set it when behind a proxy that changes the scheme/host/port.
# HONE_PUBLIC_URL=https://core.example.com

# Gather sources to pull patchsets from — comma-separated, in priority order
# (optional). Empty / unset means every installed source. Installed sources:
# lore (the canonical patchset source — needs a public-inbox archive cloned,
# see SOURCES.md and HONE_LORE_AUTOCLONE below).
# This (and the other tunables) only *seeds* config.yaml on first start;
# thereafter config.yaml is authoritative — edit it via the operator UI's
# Settings page (ARCHITECTURE.md → Configuration).
# HONE_GATHER_SOURCES=lore

# Lore archive: opt-in background autoclone on hone-core startup. With this
# set, hone-core runs the bundled `clone` helper in a background task on
# first start, instead of waiting for the operator to run it by hand. The
# clone is bounded by --shallow-since=<Lore.since_date>, so the download
# is the window gather will actually look at (a few hundred MB to ~1 GB
# for a recent floor — see SOURCES.md). The service comes up clean either
# way; autoclone just self-bootstraps the archive in the background.
# HONE_LORE_AUTOCLONE=1

# Source selection (precedence: HONE_LORE_LISTS, then HONE_LORE_URL; with
# neither set, a built-in wide default list set is used). There is no
# built-in default URL: lore.kernel.org doesn't git-serve the /all/
# firehose, so a blanket default URL would only fail — the default is a
# cloneable *list set* instead. The default only chooses WHICH lists to
# provision; nothing is cloned until autoclone or the Settings "Provision
# now" button runs.
#
# HONE_LORE_URL — a single list (point at a specific epoch) or a private
# mirror. Read by the `clone` helper and the autoclone path.
# HONE_LORE_URL=https://lore.kernel.org/linux-arm-msm/0
#
# HONE_LORE_LISTS — wide coverage across many subsystem lists (takes
# precedence over HONE_LORE_URL). lore git-serves only the individual
# lists, so each is cloned separately (one per-list clone under
# HONE_ARCHIVE_DIR/lore/<list>, current epoch auto-discovered, per-list
# resume cursor). Defaults to the set below when unset; override to taste.
# HONE_LORE_LISTS=netdev,linux-mm,dri-devel,linux-pci,linux-fsdevel,linux-arm-msm
