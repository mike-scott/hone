"""hone-core — the hone control plane.

A non-AI FastAPI service: runs the GATHER cron in-process, serves the v1 REST
API for nodes and the operator web UI, and owns the database. See
../docs/ARCHITECTURE.md and ../docs/API.md.
"""
