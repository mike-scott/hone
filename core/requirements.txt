# hone-core — Python dependencies.  Install:  pip install -r core/requirements.txt
# SQLite is in the Python standard library — no dependency needed.
# Floors only; a deployment should pin (pip freeze) or lock these.
fastapi>=0.115             # web framework — the v1 REST API and the UI routes
starlette>=0.47            # fastapi's core; >=0.47 has status.HTTP_422_UNPROCESSABLE_CONTENT
uvicorn[standard]>=0.32    # ASGI server (the Dockerfile CMD runs uvicorn)
pydantic>=2.9              # typed request/response models (see common/)
jinja2>=3.1                # server-rendered HTML templates (operator web UI)
pyyaml>=6.0                # methodology import/export, config files
jsonschema>=4.23           # validate a methodology against methodology.schema.yaml
mdformat>=1.0              # canonicalize methodology prose Markdown
python-multipart>=0.0.12   # form posts + file upload (operator UI, manual submissions)
cryptography>=43           # first-start TLS CA + server certificate generation
zstandard>=0.23            # pack gathered patchsets into .tar.zst archives
argon2-cffi>=23            # Argon2id password hashing for operator accounts
httpx>=0.28                # async HTTP client for Google OAuth token exchange
itsdangerous>=2.2          # signs session cookies (used by SessionMiddleware)
